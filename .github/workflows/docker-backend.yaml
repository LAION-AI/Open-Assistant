name: (Backend) Publish Docker Image

on:
  push:
    paths:
      - "backend/**"

jobs:
  build:
    uses: ./.github/workflows/docker-build.yaml
    with:
      image-name: backend
      folder: backend
      build-args: ""
