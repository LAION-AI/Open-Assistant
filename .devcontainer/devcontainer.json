{
  "name": "Open-Assistant",
  "image": "mcr.microsoft.com/vscode/devcontainers/universal",
  "initializeCommand": "pip install pre-commit && (cd website/ && npm install) && docker compose build" 
}
